package main

import (
    "flag"
    "fmt"
    "log"
)

var n = flag.Int("n",0,"numbers")
var s = flag.String("s"," ","separator")
var b = flag.Bool("b",false,"omit trailing newline")

func main() {
    flag.Parse()
    fmt.Println(*s)
    fmt.Println(*n)
    fmt.Println(*b)
}
